+1. Работа счетчика для товара добавленного в корзину.

2.Избежать дуюлирование товара в корзине

2.1 Проверять есть ли уже такой товар в корзине
2.2  Если есть ➤ тогда увеличиваем его количество
2.3 Если нету то добавляем как обычно

2.4 Сбрасывать чсетчик товара на 1



3.Возможность удаления товара

4.Подсчет стоимости заказа, который сдесь отображается

5.Если заказ больше 1000 рублей, стоимость доставки ➤ Бесплатно, иначе будет стоимость доставки




// ? Даем название для функции чтобы можно было её записать несколько раз сокращенно
    function clickFunction() {
      // Находим div с счетчиком
      let counterWrapper = this.closest('.counter-wrapper');
      let counter = counterWrapper.querySelector('[data-counter]');

      // 3. Для кнопки '+' -> Увеличиваем
      if (this.dataset.action === "plus") {
        counter.innerText++;
      } else {
        // 4. Для кнопки '-' -> Проверяем на > 1 и Уменшаем
        if (parseInt(counter.innerText) > 1) {
          // действие, если условие выполняется
          counter.innerText--;
        }
      }
    }